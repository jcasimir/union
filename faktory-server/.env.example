# Faktory Server Configuration
# Copy this to .env and customize

# Your server's Tailscale IP (find with: tailscale ip -4)
# This ensures Faktory only listens on the Tailscale network
TAILSCALE_IP=100.x.x.x

# Strong password for worker authentication
# Generate one with: openssl rand -hex 16
FAKTORY_PASSWORD=your-secure-password-here
